require "spec_helper"

describe CofirestDatabase do
  it "has a version number" do
    expect(CofirestDatabase::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
